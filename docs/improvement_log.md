# Improvement log — red-team remediation campaign

Governance: the measuring stick is FROZEN. `git diff` on tests/** and evals/** must be
additions-only; eval datasets/labels/thresholds/split/grading logic untouched. Grades may
move only because the product improved. Measurement bugs go to docs/eval_harness_issues.md,
never silently fixed. No fixture results cited as quality evidence; verification is live.

Baseline (pre-campaign): red-team grade C+ (2026-07-13). Verified-true findings:
report.py assignment-composer dict-slice crash (Gusto run, error-path render recovered);
crashed runs can show status=complete; exa_agent 150s poll inside 60s tool boundary (live
research_linkedin dead); temporal judge fed bare artifact IDs (everything low-confidence);
plan_research route has zero call sites (no LLM in the planning loop); no adversarial news
layer (Rippling–Deel litigation invisible); focal-lacks-X claims unverified against the
focal corpus (3 false premises in flagship recommendations); zero live ad creatives
(Meta needs token; Google Transparency needs a browser path; Exa can't index facebook.com).

| date | item | outcome | evidence | spend |
|---|---|---|---|---|
| 2026-07-13 | P0-3 exa_agent tool self-timeout | fixed | Per-tool boundary override `TOOL_TIMEOUT_SECONDS` added to `BaseTool` (base.py:149, default None = global 60s unchanged), honored at the boundary (base.py:393-400). Boundary self-timeout made non-retryable via `asyncio.timeout(...).expired()` so a doomed call no longer burns 3x; a raw provider TimeoutError stays retryable (base.py:401-410). exa_agent=190 (exa_agent.py:132, > its 150s poll budget), similarweb=150 (similarweb.py:164). Trace: with the override, exa_agent's 150s poll budget < 190s boundary, so the poller returns a typed result before the boundary — the mismatch disappears; the non-retryable-timeout guard defends the residual case. LIVE VERIFY run `RUN-live-verify-item3`: research_linkedin deel.com through registry + live ToolContext with `tool_timeout_seconds=60` (old default) → status=success at 62.0s wall (>60s: the old boundary would have killed it, matching red-team 13/13), 11 artifacts (10 linkedin_post + 1 synthesis), cost $0.4764, BOUNDARY_TIMEOUT_HIT=False. Tests: tests/unit/test_tool_timeout.py (8 cases incl. 1s-budget stub self-timeout @~1s non-retryable, exa_agent attr ≥ poll budget computed from `_POST_TIMEOUT`+`_MAX_POLLS`*`_POLL_INTERVAL`); 334 unit+contract green. | $0.4764 |
| 2026-07-13 | P0-1 assignment-composer dict-slice crash | fixed | Root cause: `build_assignment_answers` sliced untrusted classifier/opportunity free-text with `[:220]`/`[:180]`/etc. The only `[:220]` reading an LLM-authored classification field was `example_message = (best.get("primary_message") or "")[:220]` (was report.py:1899) — a `primary_message` arriving as a wrapped object (`{"text"/"label"/"statement": ...}`) is truthy, so `or ""` doesn't guard, and `dict[:220]` raises `KeyError: slice(None, 220, None)`, matching the trace exactly (RUN-a1c097c49751 node `run_focal_mirror_check`; mirror RUN-6cacb71ad0b2 node `render_outputs`, both `KeyError: slice(None, 220, None)` in trace.jsonl). The persisted store is now clean — repository re-validates every str field on read (`model_validate_json`, `extra="forbid"` at common.py:63), so a dict can't round-trip; the error-path re-render sanitized it (confirmed: 0 dict-shaped `primary_message`/`claim_text`/`competitor_pattern`/etc. across all 2477 merged classifications + 763 opp/gap rows, all runs) → bug was latent, not surfaced by current data. Fix by construction: module-level `_coerce_text(value)->str` (report.py:1805; str passthrough, dict→text/label/statement str else compact json.dumps, list→space-joined coerced items, None→"", scalar→str) routed through EVERY untrusted-string slice/format in the composer (report.py: exact_excerpt[:200], _theme_key, Q1 theme grouping + example_message[:220] + linkedin theme/excerpt[:180], dom theme lookup, villain wording, gap their_claim[:180]+justification[:220], play justification[:240]). VERIFY against reality: `uv run competitive-agent render RUN-6cacb71ad0b2` and `RUN-a1c097c49751` both exit 0; both data.json carry `assignment_answers` (all 4 q-keys) with citations 60 and 43 respectively, no dict-repr leak, and both briefs contain the deliverable section "## The assignment deliverable — four questions". Tests (append-only): tests/unit/test_report.py::test_assignment_answers_coerce_dict_shaped_fields_no_crash (dict primary_message + dict opportunity field + dict villain items + dict claim_text → clean strings, render succeeds), ::test_coerce_text_branches; 337 unit+contract green; ruff+format+mypy clean on report.py and test_report.py. | $0 |
| 2026-07-13 | P0-2 crashed runs reported truthfully | fixed | Root cause: `graph.py::_checkpoint` wrote `status="complete" if state.is_complete else "running"`, but the node-failure `except` sets `is_complete=True` (to enable an error-path render on resume) before checkpointing — so a crashed node was persisted `complete`. Fix: `_checkpoint` now derives `status='complete'` ONLY when `current_node=='render_outputs_done'` (the genuine terminal), else `'running'`, and the failure path passes `status='failed'` explicitly with `current_node` preserved at the dead node (graph.py:107,119-141). Out-of-graph failures persisted durably via `api.py::_persist_run_failure` (never clobbers a terminal row, api.py:113-133) wired into all 3 job threads (_run_job/_resume_job/_research_job, api.py:163/199/238). `GET /api/runs` surfaces failed runs both with a salvaged report (`"failed": true`, api.py:501-522) and without (`live_status="failed"`, api.py:464-483); `/live` returns `"failed"` (api.py:615). VERIFY against reality (verifier): both morning rows now read DB status='failed' with preserved current_node + honest stop_reason (RUN-a1c097c49751 Gusto → run_focal_mirror_check / node_error:...:KeyError; RUN-6cacb71ad0b2 rippling.com → render_outputs / node_error:...:KeyError); TestClient `GET /api/runs` lists both with `failed=true` + honest stop_reason; `/live` returns `status=failed` for both. Tests (new file, append-only): tests/behavior/test_crashed_run_reporting.py (4 tests, all pass) incl. a monkeypatched node crash → DB failed everywhere + a happy-path guard that a genuine run still reaches render_outputs_done→complete. | $0 |
| 2026-07-13 | P0-4 temporal judge sees real evidence + dedup | fixed | Root cause: `temporal.py::build_change_events` rendered the judge's prior/current evidence blocks as bare `- ART-xxx` id lines while the template promised "PRIOR-PERIOD EVIDENCE" text — the judge had nothing to verify, so events came back low-confidence. Fix: `_render_evidence_block` renders titled/dated/excerpted lines `- [title] (source_type, date): <~300-char excerpt>` off the real `RawArtifact` objects loaded via `repository.list_artifacts(run_id=...)` (temporal.py:301 dict), bounded to 8 artifacts/window with honest truncation + `+N more`; window_sample prior blocks get a role NOTE (temporal.py:223+). Dedup added at three points: `_dedup_candidates` before judging (temporal.py:313), `emitted_keys` guard after judging (temporal.py:333/432), and a final dedup on `reconcile_change_events` output — the exact brief render input (temporal.py:628). Prompt `temporal_judge_v1.md` bumped 1.0.0→1.1.0 with the new line-format instructions. VERIFY (verifier): rendered the REAL judge prompt (real PromptRegistry, version 1.1.0) for a crafted prior/current candidate → prior+current excerpts present under the PRIOR/CURRENT headers, titled+dated line present, NO bare `- ART-xxx` line; `_dedup_candidates` collapses 2 identical of 3 candidates → 2. Tests (new file, append-only): tests/unit/test_temporal_evidence.py (8 tests, all pass). Note: mechanical wiring proof only; the confidence-level lift needs a live both-period candidate reaching the now-evidence-fed judge. | $0 |
| 2026-07-13 | Ask-AI panel + tool-called viz (independent verify + fix) | verified + fixed | FROZEN audit: `git diff HEAD -- tests/ evals/` additions-only (tests/behavior/test_chat.py +160/-0; test_chat_viz.py + plan_research fixtures untracked adds; evals untouched). HONESTY (real flagship RUN-b256fab1c1dd): independent recompute of two chart_types matched `build_visualization` EXACTLY — theme_distribution competitor-scoped (Deel CO-0cf92b8592a5) top rows consolidation=39, automation=14, compliance=10, … over 120 themed tags / 17 distinct; cep_ownership 23 CEPs in shares-mode both series identical; unknown_chart_type / empty_data / invalid_group_by_field all return typed `{error,error_type}` with NO `data` key (never a fabricated row). CONTEXT: api.py `ChatRequest.context` → `chat_about_run(context=…)` → `_element_note` ("THE USER IS ASKING ABOUT THIS ELEMENT"); behavior test recompute + API-context test green. FIX (bug the smoke missed): VizSpec read `spec.columns/rows/cells/series` at top level but chat_viz nests them under `spec.data` — proven by rendering real backend specs through the actual component (esbuild + react-dom/server): grouped_bar & table THREW `TypeError: (…||[]).map is not a function` and heatmap rendered "No matrix data." (no error boundary → white-screen for 4 of 8 chart types). Fixed VizSpec in ui/src/charts.jsx to unwrap the nested data shape (data.{groups,series} / {columns,rows} / {rows,cols,cells}) while keeping flat-shape back-compat; re-render proof: all 4 backend shapes + flat render cleanly. Strengthened scratchpad smoke2.mjs to feed nested grouped_bar/table/heatmap specs and assert `.gbar-fill` / a real `.viztable td` / `.hm-cell` render. GATES: `npm run build` OK; smoke = "SMOKE OK"; ruff check + mypy(86 files) clean, owned files ruff-format clean; `uv run pytest -q` = 419 passed. | $0 |
| 2026-07-13 | Items 6+7: adversarial news / focal-claims gate / industry lens wired (independent verify) | verified | Closes the baseline findings "no adversarial news layer (Rippling–Deel litigation invisible)" and "focal-lacks-X claims unverified against the focal corpus (3 false premises in flagship recommendations)", plus the industry-adaptivity gap. LIVE CONSTRAINT: Anthropic account usage-limited until 2026-08-01, so verification is FIXTURE + UNIT only (no live run) — disclosed here, not cited as quality evidence for grade movement. FROZEN audit: `git diff HEAD -- tests/ evals/` = zero deletions, and `git diff HEAD --stat -- tests/ evals/` = empty (no tracked test/eval modified); all new tests are untracked adds (test_batch2b_wiring.py, test_focal_gate.py, test_industry.py, test_news_tool.py + 2 fixtures). WIRING traced in code, not trusted: (a) `NewsTool` registered once in `runner._build_registry` (runner.py:47), proposed once via `planner._optional("news_market", …, {company,domain,focal})` (planner.py:660) gated on the `sources.news_market` flag; coverage lands only on real dated artifacts (`_ACTION_COVERAGE_EVIDENCED["search_market_context"]→[("market_context","medium")]`, nodes.py:98); (b) `industry.infer_industry_context` awaited in `build_matrices` via `_infer_industry_context` (nodes.py:1067/1127), stored on `state.industry_context`, wrapped in try/except that leaves None on any failure (never crashes); readiness gate correctly filters focal_* dims out of `required_dimensions(mode, compare=False)` — verified that comparative mode still injects focal_current/focal_proof at coverage.py:120, which the mirror raises only AFTER build_matrices, so the filter is necessary to avoid deadlock; (c) `focal_gate.verify_focal_claims` called in `critique_opportunities` (nodes.py:1229/1355) — AFTER `run_focal_mirror_check` (graph NODE_ORDER 40→42), reads focal corpus via report.py `_focal_classifications`/`_focal_artifact_models` off `state.focal_run_id`; a `contradicted` verdict DROPS the opportunity (repo REPLACE + `state.opportunity_ids` pruned + limitation appended) and SOFTENS the proof_gap in place (attackability→low, detail.overall→concede, `[withdrawn…]` prefix) — not just logged; (d) `synthesis.market_context` is pure counting over `news_market` artifacts (no model call); (e) report renders both `## Industry lens` and `## Market context — what is happening TO {competitor}`, plus a Limitations withdrawal disclosure. INDEPENDENT FIXTURE RUN (verifier, keyless, isolated tmp db): `run_analysis deel.com comparative fixture compare rippling.com` = RUN-66f9cf91b61d, stop=required_coverage_reached, complete=True. data.json: `market_context.total=3` (litigation/funding/launch, counts sum == total, every item shape == {category,title,url,published_at}), `source_distribution.news_market=3`, `coverage.market_context=medium`, `industry_context.industry="security & compliance automation"` (== state.industry_context), `focal_gate_findings` present (empty = honest: fixture opps carry no "focal lacks X" phrasing). brief.md contains both new sections + the industry string. INDEPENDENT GATE CRAFT (verifier, repo-backed, drives nodes.critique_opportunities directly): a false-premise opportunity ("Rippling has no goals product") AND a false-premise proof_gap ("Rippling does not have a goals product") + a focal rippling.com/goals page → BOTH get a `contradicted` verdict (x_phrase "goals product", evidence cites rippling.com/goals); the opportunity is DROPPED from state + store while the clean opp survives; the proof_gap is SOFTENED in place (attackability low, overall concede, withdrawal prefix) while the clean gap is untouched (attackability high, overall attack); withdrawal disclosed in state.limitations; re-run is IDEMPOTENT (interp unchanged, opp set stable, single prefix). GATES green (fixture-based, no live): `uv run pytest -q` = 461 passed; batch-2b targeted = 25 passed; `ruff check` + `ruff format --check` clean (90 files); `mypy` clean (89 files); `eval --suite contracts` = 401 passed; `demo-check --mode fixture` = DEMO CHECK OK (RUN-b6a339ef8255). No test weakened; no source fix required — builder report matched reality on every checked claim. | $0 (keyless; account usage-limited to 2026-08-01) |
| 2026-07-13 | Item 5: LLM IN the planning loop (independent verify) | verified | Closes the baseline finding "plan_research route has zero call sites (no LLM in the planning loop)". FROZEN audit: `git diff HEAD -- tests/ evals/` = empty, zero deletions; the change adds only untracked tests/unit/test_llm_planner.py + tests/fixtures/model/plan_research/default.json (evals untouched). CONTRACT traced in code, not trusted: (a) the model is CONSTRAINED to the deterministic candidate set — `planner._resolve_choice` maps an out-of-range `chosen_index` or an unknown action_type/source to None, so an invented action can never execute (planner.py `_resolve_choice`; tests test_chosen_index_out_of_range_returns_none, test_action_not_in_candidates_returns_none); (b) `model_rationale` (the model's own prose) is threaded into the `action_selected` trace with `decision_by:"model"`, distinct from the rule rationale (nodes.py `select_next_action`); (c) real deterministic FALLBACK — `planner.llm_plan_selection` returns None on any gateway error/timeout/invalid-output/unresolvable-choice, and `nodes.score_actions` keeps the deterministic top scorer with `decision_by:"heuristic"`; (d) gated by config flag `execution.llm_in_the_loop` (default true; flag off ⇒ gateway never called — test_score_actions_flag_off_never_calls_gateway); (e) a model STOP is floored by `required_dims_needing_exhaustion` so the model can never waive a required dimension. GATES: test_llm_planner.py 17/17 pass; ruff + `ruff format --check` + mypy clean on planner.py + nodes.py; 40 planner-adjacent tests (planner_pricing/loop_rebalance/coverage) green. INDEPENDENT LIVE PROOF (not a builder run): `analyze workday.com --compare rippling.com --execution-mode live` = RUN-678fd9d5f89d. 7 live model plan decisions (12:51:51–12:56:02 UTC), and the model DIVERGED from the deterministic top-scorer 4× (chose search_exa_web, search_wayback, search_news_launches, search_comparison_pages over the heuristic's fetch_webpage) — genuine agency, not cosmetic. Quoted model_rationale (search_exa_web step, verbatim): "Pricing_and_packaging is a required dimension currently untouched, and it's high-value for a comparative competitive question (buyers care about pricing/packaging differences). Candidate [2] directly targets this gap via a fresh web search rather than repeating the webpage_fetch that only got 1 page done or diving into wayback history before we've even established current-state coverage." SEQUENCE DIVERGENCE (different competitors → different research paths): workday = [map_current_website, search_exa_web, search_wayback, search_news_launches, search_comparison_pages, fetch_webpage×6, …breadth sweep] vs builder rippling RUN-0f879f9b6827 = [map_current_website, fetch_webpage, search_news_launches → model-stop] vs builder deel RUN-0741f63cafbb = [map_current_website, search_exa_web, fetch_webpage, search_news_launches → model-stop]; both builder runs' `stop_selected` carry `decision_by:"model"` + `model_judged_complete` rationale. FALLBACK PROVEN UNDER REAL CREDIT EXHAUSTION: the Anthropic account usage limit hit mid-run at 12:56:03 (resets 2026-08-01); the remaining 17 decisions correctly fell to `decision_by:"heuristic"` and the loop continued deterministically — the fallback is real, exercised live. DETERMINISM (keyless): fixture comparative RUN-1ca3c63bb1dc reached terminal stop `required_coverage_reached` + brief.md (368 lines) with 18 deterministic model-path decisions. OUT-OF-SCOPE note (not this change, not a blocker): RUN-678fd9d5f89d later crashed in `run_focal_mirror_check` with `TypeError: can't compare offset-naive and offset-aware datetimes` — a pre-existing datetime bug in a node the diff never touches (builder deel/rippling live runs of this same feature rendered briefs fine); logged here, not fixed, to avoid scope-creep into the concurrent track. No test weakened; no fix applied to the planner change. | ~live spend bounded (< $3/run cap; account monthly usage limit reached mid-run, did not retry-spend) |
| 2026-07-13 | Polish/robustness FIX 1: offset-naive vs offset-aware datetime crash | verified | Independent verify of the fix for the pre-existing `TypeError: can't compare offset-naive and offset-aware datetimes` that took down `run_focal_mirror_check` on live workday.com (logged out-of-scope in the Item-5 row above). Root cause confirmed in code: `synthesis.assign_window` (the single window-membership predicate every temporal/baseline path routes through) compared a naive artifact date (`archive_capture_at`/`published_at`, naive from some sources) against tz-aware window bounds + `utcnow()`. Fix `synthesis._as_aware(dt)` (naive→UTC, aware passthrough preserving offset) is applied at the REAL comparison site — synthesis.py:104-110 coerces BOTH the artifact date AND every window bound (`comparison.start_at/end_at`, `current.start_at`) before any `<=`/`>=`, so a naive date can never reach a naive-vs-aware compare; verified it is the only direct date comparison in the owned files (other date uses are display `.date().isoformat()` / ISO-string sort). VERIFY (verifier ran): tests/unit/test_polish_robustness.py 14/14 green — `test_naive_vs_aware_raw_comparison_is_the_pre_fix_crash` asserts the pre-fix `aware_bound <= naive` genuinely raises the exact TypeError; post-fix `assign_window` with naive `published_at`→prior/current, naive `archive_capture_at`→prior, aware happy-path unchanged (current/prior/outside), `temporal_baseline` over a naive-dated corpus counts prior=1/current=1 without crashing, `_as_aware` naive→UTC + aware-offset-preserved branches. No test weakened. | $0 (keyless; fixture + unit only; account usage-limited to 2026-08-01) |
| 2026-07-13 | Polish/robustness FIX 2: provider circuit breaker | verified | Independent verify of the fix for the red-team finding "after Exa returned 402 out-of-credits the loop scheduled 3 more Exa-backed tools into the dead provider (runtime_exhausted)". Mechanism confirmed in code: additive persistence-safe `state.dead_providers: list[str]`; `planner._SOURCE_PROVIDER` maps every Exa-backed source (exa_search/exa_agent/exa_contents/similarweb/reviews/jobs/events/ooh/news_market/meta_ads/linkedin_ads)→`exa` and google_ads→`serpapi`, leaving public-HTTP tools (wayback/webpage_fetch/website_map) unmapped/never broken; `TERMINAL_PROVIDER_ERRORS={provider_out_of_credits (402), provider_auth (401/403)}`; `propose_actions.allowed()` (planner.py:363-374, the single choke point feeding both heuristic and LLM candidate paths) drops any proposal whose source maps to a dead provider; `nodes.execute_action` (nodes.py:459-477) records a newly-dead provider once, appends a one-time disclosure to BOTH `negative_observations` and `limitations`, and emits a `provider_circuit_open` trace, all guarded so a second 402 neither re-records nor re-discloses. VERIFY (verifier ran): `test_propose_actions_skips_dead_provider_but_keeps_others` proves exa actions are normally proposed (precondition) then fully dropped when exa is dead while non-exa survive; `test_execute_action_402_marks_provider_dead_and_discloses_once` proves two consecutive Exa 402s → `dead_providers==["exa"]` and exactly ONE disclosure in each channel; retryable 429/5xx + success + non-provider public tools do NOT trip the breaker. No test weakened. | $0 (keyless; fixture + unit only; account usage-limited to 2026-08-01) |
| 2026-07-13 | Polish/conversational: dig-deeper always-research + run-again spawns a new run | verified | Independent verify of two follow-up-chat behaviors. FIX A — `chat.py` `CHAT_SYSTEM` gained a DIG DEEPER = ALWAYS RESEARCH block: an explicit "dig deeper / go deeper / research more on {topic}" always emits `research_request` even with partial coverage, with topic→sources map (pricing→[web,wayback], ads→[ads], reviews→[reviews], social/linkedin→[linkedin], news/funding/lawsuit→[news], keywords→[keywords], else→[web]); the fully-answerable-factual guard is preserved. FIX B — new `SpawnRun{company, compare_to?}` schema + `ChatResponse.spawn_run: SpawnRun|None=None`; a RUN A DIFFERENT COMPANY prompt block; `api.py` `POST /api/runs/{id}/spawn` (SpawnBody) that 404s on unknown parent, 400s on missing company/bad execution_mode, inherits the parent's focal (`compare_to` from `state_json`) + execution_mode + mode (clamped to comparative if the parent's stored mode isn't UI-allowed), then delegates to the existing `create_run`/`_run_job` machinery. VERIFY (verifier ran): tests/behavior/test_chat.py 25/25 green — `test_chat_dig_deeper_on_pricing_emits_research_request` (prompt teaches DIG DEEPER+pricing/wayback; dict returns research_request sources ["web","wayback"], needs_deeper_research=True, spawn_run None), `test_chat_run_again_for_company_emits_spawn_run` (spawn_run{Gusto,Rippling} round-trips; prompt teaches the rule), `test_spawn_endpoint_starts_new_run_and_guards` (POST spawn polls `_JOBS` → a NEW run_id distinct from the parent inheriting compare_to="rippling.com"; 404 unknown parent, 400 blank company), `test_chat_response_spawn_run_defaults_none_and_fixture_validates`. No eval/existing-test line modified (additions-only). No test weakened. | $0 (keyless; fixture + monkeypatched-gateway only; account usage-limited to 2026-08-01) |
| 2026-07-13 | interactive-vercel: DEMO_PUBLIC + VITE_API_BASE + Docker/render | verified | Independent verify of the hosted-interactive deploy: a Vercel UI chats/browses against a hosted FastAPI without letting the public trigger the ~$7 live analyses. FROZEN audit: `git diff HEAD -- tests/ evals/` has ZERO deletion lines (tests/behavior/test_chat.py +113/-0; evals untouched); the only source edits are api.py (+48/-1, the -1 being the widened config import) and ui/src/App.jsx (+76/-21). BACKEND (TestClient, DEMO_PUBLIC toggled hermetically via os.environ — the running :8000 server was never touched): `_DEMO_BLOCK_DETAIL` matches the required string EXACTLY; `GET /api/health`→200 `{status:ok, demo_public:false}` unset / `true` when `=1`. With `DEMO_PUBLIC=1` all 5 expensive POSTs 403 with that exact detail (create /api/runs, /spawn, /research, /paid-search, /product-focus — gate is the first statement so no work starts); GET /api/runs still lists a seeded fixture run and POST /{id}/chat still 200s with a grounded answer; with the gate off POST /api/runs → 200 pending job. Gate reads env at request time via `secret_from_env_or_settings` (config.py untouched, no demo_public field needed → unset behaves exactly as before). The 3 appended tests pass; the pre-existing 5 gated-endpoint tests still pass with the gate off. DEPLOY ARTIFACTS: Dockerfile (python:3.12-slim, uv two-stage sync, COPYs src/config/ui/public/demo/scripts, entrypoint `python scripts/seed_demo_runs.py && exec uvicorn competitive_agent.api:app --host 0.0.0.0 --port ${PORT:-8000}`); render.yaml parses via yaml.safe_load (type web, env docker, healthCheckPath /api/health, autoDeploy false, DEMO_PUBLIC="1" + ANTHROPIC/EXA/SERPAPI keys as sync:false); .dockerignore excludes `.env`/`.env.*` but keeps ui/public/demo. SEED→SERVE proven end-to-end in an ISOLATED OUTPUTS_DIR+DB_PATH (fresh-container model): seed copies 3 real demo packages (idempotent — 2nd run skips all 3), and GET /api/runs returns EXACTLY those 3, each read=200 + brief=200, while a gated write 403s. (A first pass showed 14 runs because only OUTPUTS_DIR was isolated, not db_path — stale in-flight rows from the shared real DB; isolating the DB confirmed the disk-scan block, which runs regardless of DB state, surfaces exactly the 3 seeded runs.) FRONTEND: `npm run build` (no base) OK + scratchpad/smoke2.mjs = "SMOKE OK" (DEMO_MODE false, deliverables hrefs are literal `/api/runs/RUN-done1/brief` etc. — buildUrl is a pure passthrough), bundle contains no base string; `VITE_API_BASE=https://example-api.test npm run build` bakes `Io="https://example-api.test".replace(/\/+$/,"")` + `function ge(e){return Io?Io+e:e}` and wraps all 17 /api call sites incl. `ge("/api/runs")` (probe), chat, jobs, live, research, brief — base concatenated into the fetch path; canonical no-base dist restored (0 base strings). GATES green (fixture-based): ruff check + `ruff format --check` (140 files) + mypy (89 files) all clean; `uv run pytest -q` = 482 passed; `eval --suite contracts` = 415 passed; `demo-check --mode fixture` = DEMO CHECK OK. No test weakened; no source fix required — both builder reports matched reality on every checked claim. | $0 (keyless; fixture + TestClient only; live :8000 Deel run left untouched) |

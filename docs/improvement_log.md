# Improvement log — red-team remediation campaign

Governance: the measuring stick is FROZEN. `git diff` on tests/** and evals/** must be
additions-only; eval datasets/labels/thresholds/split/grading logic untouched. Grades may
move only because the product improved. Measurement bugs go to docs/eval_harness_issues.md,
never silently fixed. No fixture results cited as quality evidence; verification is live.

Baseline (pre-campaign): red-team grade C+ (2026-07-13). Verified-true findings:
report.py assignment-composer dict-slice crash (Gusto run, error-path render recovered);
crashed runs can show status=complete; exa_agent 150s poll inside 60s tool boundary (live
research_linkedin dead); temporal judge fed bare artifact IDs (everything low-confidence);
plan_research route has zero call sites (no LLM in the planning loop); no adversarial news
layer (Rippling–Deel litigation invisible); focal-lacks-X claims unverified against the
focal corpus (3 false premises in flagship recommendations); zero live ad creatives
(Meta needs token; Google Transparency needs a browser path; Exa can't index facebook.com).

| date | item | outcome | evidence | spend |
|---|---|---|---|---|
| 2026-07-13 | P0-3 exa_agent tool self-timeout | fixed | Per-tool boundary override `TOOL_TIMEOUT_SECONDS` added to `BaseTool` (base.py:149, default None = global 60s unchanged), honored at the boundary (base.py:393-400). Boundary self-timeout made non-retryable via `asyncio.timeout(...).expired()` so a doomed call no longer burns 3x; a raw provider TimeoutError stays retryable (base.py:401-410). exa_agent=190 (exa_agent.py:132, > its 150s poll budget), similarweb=150 (similarweb.py:164). Trace: with the override, exa_agent's 150s poll budget < 190s boundary, so the poller returns a typed result before the boundary — the mismatch disappears; the non-retryable-timeout guard defends the residual case. LIVE VERIFY run `RUN-live-verify-item3`: research_linkedin deel.com through registry + live ToolContext with `tool_timeout_seconds=60` (old default) → status=success at 62.0s wall (>60s: the old boundary would have killed it, matching red-team 13/13), 11 artifacts (10 linkedin_post + 1 synthesis), cost $0.4764, BOUNDARY_TIMEOUT_HIT=False. Tests: tests/unit/test_tool_timeout.py (8 cases incl. 1s-budget stub self-timeout @~1s non-retryable, exa_agent attr ≥ poll budget computed from `_POST_TIMEOUT`+`_MAX_POLLS`*`_POLL_INTERVAL`); 334 unit+contract green. | $0.4764 |
| 2026-07-13 | P0-1 assignment-composer dict-slice crash | fixed | Root cause: `build_assignment_answers` sliced untrusted classifier/opportunity free-text with `[:220]`/`[:180]`/etc. The only `[:220]` reading an LLM-authored classification field was `example_message = (best.get("primary_message") or "")[:220]` (was report.py:1899) — a `primary_message` arriving as a wrapped object (`{"text"/"label"/"statement": ...}`) is truthy, so `or ""` doesn't guard, and `dict[:220]` raises `KeyError: slice(None, 220, None)`, matching the trace exactly (RUN-a1c097c49751 node `run_focal_mirror_check`; mirror RUN-6cacb71ad0b2 node `render_outputs`, both `KeyError: slice(None, 220, None)` in trace.jsonl). The persisted store is now clean — repository re-validates every str field on read (`model_validate_json`, `extra="forbid"` at common.py:63), so a dict can't round-trip; the error-path re-render sanitized it (confirmed: 0 dict-shaped `primary_message`/`claim_text`/`competitor_pattern`/etc. across all 2477 merged classifications + 763 opp/gap rows, all runs) → bug was latent, not surfaced by current data. Fix by construction: module-level `_coerce_text(value)->str` (report.py:1805; str passthrough, dict→text/label/statement str else compact json.dumps, list→space-joined coerced items, None→"", scalar→str) routed through EVERY untrusted-string slice/format in the composer (report.py: exact_excerpt[:200], _theme_key, Q1 theme grouping + example_message[:220] + linkedin theme/excerpt[:180], dom theme lookup, villain wording, gap their_claim[:180]+justification[:220], play justification[:240]). VERIFY against reality: `uv run competitive-agent render RUN-6cacb71ad0b2` and `RUN-a1c097c49751` both exit 0; both data.json carry `assignment_answers` (all 4 q-keys) with citations 60 and 43 respectively, no dict-repr leak, and both briefs contain the deliverable section "## The assignment deliverable — four questions". Tests (append-only): tests/unit/test_report.py::test_assignment_answers_coerce_dict_shaped_fields_no_crash (dict primary_message + dict opportunity field + dict villain items + dict claim_text → clean strings, render succeeds), ::test_coerce_text_branches; 337 unit+contract green; ruff+format+mypy clean on report.py and test_report.py. | $0 |
| 2026-07-13 | P0-2 crashed runs reported truthfully | fixed | Root cause: `graph.py::_checkpoint` wrote `status="complete" if state.is_complete else "running"`, but the node-failure `except` sets `is_complete=True` (to enable an error-path render on resume) before checkpointing — so a crashed node was persisted `complete`. Fix: `_checkpoint` now derives `status='complete'` ONLY when `current_node=='render_outputs_done'` (the genuine terminal), else `'running'`, and the failure path passes `status='failed'` explicitly with `current_node` preserved at the dead node (graph.py:107,119-141). Out-of-graph failures persisted durably via `api.py::_persist_run_failure` (never clobbers a terminal row, api.py:113-133) wired into all 3 job threads (_run_job/_resume_job/_research_job, api.py:163/199/238). `GET /api/runs` surfaces failed runs both with a salvaged report (`"failed": true`, api.py:501-522) and without (`live_status="failed"`, api.py:464-483); `/live` returns `"failed"` (api.py:615). VERIFY against reality (verifier): both morning rows now read DB status='failed' with preserved current_node + honest stop_reason (RUN-a1c097c49751 Gusto → run_focal_mirror_check / node_error:...:KeyError; RUN-6cacb71ad0b2 rippling.com → render_outputs / node_error:...:KeyError); TestClient `GET /api/runs` lists both with `failed=true` + honest stop_reason; `/live` returns `status=failed` for both. Tests (new file, append-only): tests/behavior/test_crashed_run_reporting.py (4 tests, all pass) incl. a monkeypatched node crash → DB failed everywhere + a happy-path guard that a genuine run still reaches render_outputs_done→complete. | $0 |
| 2026-07-13 | P0-4 temporal judge sees real evidence + dedup | fixed | Root cause: `temporal.py::build_change_events` rendered the judge's prior/current evidence blocks as bare `- ART-xxx` id lines while the template promised "PRIOR-PERIOD EVIDENCE" text — the judge had nothing to verify, so events came back low-confidence. Fix: `_render_evidence_block` renders titled/dated/excerpted lines `- [title] (source_type, date): <~300-char excerpt>` off the real `RawArtifact` objects loaded via `repository.list_artifacts(run_id=...)` (temporal.py:301 dict), bounded to 8 artifacts/window with honest truncation + `+N more`; window_sample prior blocks get a role NOTE (temporal.py:223+). Dedup added at three points: `_dedup_candidates` before judging (temporal.py:313), `emitted_keys` guard after judging (temporal.py:333/432), and a final dedup on `reconcile_change_events` output — the exact brief render input (temporal.py:628). Prompt `temporal_judge_v1.md` bumped 1.0.0→1.1.0 with the new line-format instructions. VERIFY (verifier): rendered the REAL judge prompt (real PromptRegistry, version 1.1.0) for a crafted prior/current candidate → prior+current excerpts present under the PRIOR/CURRENT headers, titled+dated line present, NO bare `- ART-xxx` line; `_dedup_candidates` collapses 2 identical of 3 candidates → 2. Tests (new file, append-only): tests/unit/test_temporal_evidence.py (8 tests, all pass). Note: mechanical wiring proof only; the confidence-level lift needs a live both-period candidate reaching the now-evidence-fed judge. | $0 |
